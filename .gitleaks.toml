# gitleaks 配置：继承官方默认规则集，仅对已知误报开窄口。
[extend]
useDefault = true

[allowlist]
description = "Web HTTP 集成测试仅含本地一次性假值（SESSION_SECRET 等），非真实密钥"
paths = ['''server/webServer\.delivery\.test\.cjs''', '''server/webServer\.reference\.test\.cjs''']
