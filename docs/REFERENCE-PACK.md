# Reference Pack

Reference selection is deterministic: spatial guide, component, primary, material, composition, supporting. Provider capacity is configured once and every selected/omitted asset is recorded with a reason. The image client rejects an oversized pack; it never truncates it. Wireframes and grayscale guides are structural inputs, not visual style references.

