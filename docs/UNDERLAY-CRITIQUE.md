# Underlay Critique

Critique uses a separate model context and requires evidence boxes, confidence, global UI/text scanning, and per-Slot checks. Known UI/text contamination and subject overlap are critical; busy Slots and low confidence require review. Critical and major issues block composition. Automatic repair is capped at two attempts. A waiver must target a stable issue ID, contain a meaningful reason, and remain in the Artifact history.

