# Golden sample matrix

The repository keeps privacy-safe, deterministic contract fixtures instead of copyrighted production screenshots. Real provider acceptance remains an explicit release action.

| Sample | Purpose | Required controls | Font roles | Expected contamination |
| --- | --- | ---: | ---: | --- |
| `functional-dense` | Dense inventory/party management | 10 | 3 | busy slot, fake numeric text |
| `visual-hero` | Character-led event screen | 4 | 3 | subject crossing primary action slot |
| `existing-continuation` | Strict extension from approved pages | 8 | 4 | button residue, fake bottom navigation |

Each sample must exercise component bindings, slot policies, underlay critique evidence, composition provenance, and final fidelity. Known critical contamination must never pass automatically.

