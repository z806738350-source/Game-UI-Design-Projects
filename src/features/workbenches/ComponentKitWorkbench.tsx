import { ImagePlus, LockKeyhole } from 'lucide-react';
import { useState } from 'react';
import { copilotApi } from '../../api';
import type { DesignProject } from '../../types';

type Run = (task: () => Promise<DesignProject>, options: { label: string; stage: 'style_resolution' }) => Promise<DesignProject | undefined>;
export function ComponentKitWorkbench({ project, busy, run }: { project: DesignProject; busy: boolean; run: Run }) {
  const [id, setId] = useState('button.primary'); const [category, setCategory] = useState('button'); const [state, setState] = useState('default'); const [reuse, setReuse] = useState('exact'); const [margins, setMargins] = useState('12,12,12,12'); const [sourceBBox, setSourceBBox] = useState('0,0,0,0'); const [textPolicy, setTextPolicy] = useState('text-slot'); const [minScale, setMinScale] = useState('1'); const [maxScale, setMaxScale] = useState('2'); const [locked, setLocked] = useState('silhouette,corner-radius,border-layers,light-direction');
  const families = (project.artifacts.componentContract?.families as Array<Record<string, unknown>>) || [];
  const marginValues = margins.split(',').map(Number);
  const importAsset = () => run(() => copilotApi.importComponentAsset(project.id, { componentId: id, name: id, category, state, reuseMode: reuse, textPolicy, sourceBBox: sourceBBox.split(',').map(Number), scalePolicy: { uniform_only: reuse !== 'nine-slice', min_scale: Number(minScale), max_scale: Number(maxScale) }, ...(reuse === 'nine-slice' ? { slice: { margins: marginValues } } : {}), lockedProperties: locked.split(',').map((item) => item.trim()).filter(Boolean) }), { label: `导入 ${state} 状态资产`, stage: 'style_resolution' });
  const approve = () => run(async () => { const marked = await copilotApi.updateArtifact(project.id, 'component-contract', { families: families.map((family) => ({ ...family, status: 'approved' })) }); return copilotApi.approveArtifact(marked.id, 'component-contract'); }, { label: '验证并批准组件契约', stage: 'style_resolution' });
  return <article className="asset-workbench component-kit-workbench"><b><ImagePlus size={16} />Component Kit Workbench</b><p>逐状态导入真实透明资产；按钮、导航与页签必须具备 Default、Disabled，以及 Pressed 或 Selected。</p><div className="component-state-grid">{families.map((family) => <section key={String(family.id)}><b>{String(family.name || family.id)}</b><small>{String(family.category)} · {String(family.reuse_mode)}</small><div>{Object.entries((family.states as Record<string, Record<string, unknown>>) || {}).map(([stateId, asset]) => <i key={stateId}>{asset.preview && <img src={String(asset.preview)} alt={`${String(family.id)} ${stateId}`} />}<span>{stateId}</span><small>{String(asset.mime)} · {asset.alpha_channel ? 'Alpha' : '无 Alpha'}</small></i>)}</div></section>)}</div><label>组件 ID<input value={id} onChange={(event) => setId(event.target.value)} /></label><label>类别<select value={category} onChange={(event) => setCategory(event.target.value)}><option value="button">按钮</option><option value="navigation">导航</option><option value="tab">页签</option><option value="icon">图标</option><option value="page-specific">页面专属</option></select></label><label>状态<select value={state} onChange={(event) => setState(event.target.value)}><option value="default">Default</option><option value="pressed">Pressed</option><option value="selected">Selected</option><option value="disabled">Disabled</option></select></label><label>复用策略<select value={reuse} onChange={(event) => setReuse(event.target.value)}><option value="exact">精确复用</option><option value="nine-slice">9-Slice</option><option value="vector-token">矢量 Token</option><option value="reference-locked">参考锁定</option></select></label><label>Source BBox（x,y,w,h；0 自动取整图）<input value={sourceBBox} onChange={(event) => setSourceBBox(event.target.value)} /></label><label>文字策略<select value={textPolicy} onChange={(event) => setTextPolicy(event.target.value)}><option value="text-slot">独立文字槽</option><option value="baked">烘焙文字</option><option value="none">无文字</option></select></label><div className="scale-policy-fields"><label>最小缩放<input type="number" step="0.1" value={minScale} onChange={(event) => setMinScale(event.target.value)} /></label><label>最大缩放<input type="number" step="0.1" value={maxScale} onChange={(event) => setMaxScale(event.target.value)} /></label></div><label>锁定属性（逗号分隔）<input value={locked} onChange={(event) => setLocked(event.target.value)} /></label>{reuse === 'nine-slice' && <><label>9-Slice 边距（左,右,上,下）<input value={margins} onChange={(event) => setMargins(event.target.value)} /></label><div className="nine-slice-visual" style={{ gridTemplateColumns: `${Math.max(1, marginValues[0] || 1)}fr 3fr ${Math.max(1, marginValues[1] || 1)}fr`, gridTemplateRows: `${Math.max(1, marginValues[2] || 1)}fr 2fr ${Math.max(1, marginValues[3] || 1)}fr` }}>{Array.from({ length: 9 }, (_, index) => <i key={index}>{index === 4 ? '可伸缩中心' : index + 1}</i>)}</div></>}<div><button className="button button--secondary" disabled={busy} onClick={importAsset}>选择状态图片</button><button className="button button--secondary" disabled={busy} onClick={() => run(() => copilotApi.importForgeManifest(project.id), { label: '导入 Game UI Forge Manifest', stage: 'style_resolution' })}>导入 Forge Manifest</button><button className="button button--ghost" disabled={busy || !families.length} onClick={approve}><LockKeyhole size={14} />批准</button></div></article>;
}
